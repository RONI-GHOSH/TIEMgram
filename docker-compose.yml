version: '3.8'

services:
  backend:
    build: .
    container_name: tiemgram-backend
    ports:
      - "5000:5000"
    restart: always
    environment:
      - PORT=5000
      - DB_NAME=tiemgram
      - DB_USER=postgres
      - DB_PASS=QyCU1QES9qYMW0L11RFAsdDrQpzCSbfvY40imMUhq2baKcntOZh7itzj7GPWQOdo
      - DB_HOST=87.127.157.13
      - DB_PORT=5432
      - JWT_SECRET=your_super_secret_access_token_key_123
      - JWT_REFRESH_SECRET=your_super_secret_refresh_token_key_456
      - EMAIL_SERVICE=gmail
      - EMAIL_USER=your_email@gmail.com
      - EMAIL_PASS=your_app_password
      - NODE_ENV=production
