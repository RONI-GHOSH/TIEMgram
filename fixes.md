post get should include like counts
post like nuser list also should return like counts
get all active stories for a user with count and privacy filter
editpost should support all params which used in create post
create / editpost having option like (private/followers-following-only/public)